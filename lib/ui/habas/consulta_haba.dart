import 'package:flutter/material.dart';

class ConsultaHaba extends StatelessWidget {
  const ConsultaHaba({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Consulta')),
    );
  }
}
